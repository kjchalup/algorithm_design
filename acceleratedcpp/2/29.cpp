// Compute the maximum of two numbers.
#include <iostream>

int main(){
    int x;
    int y;

    std::cout << "Enter a number: ";
    std::cin >> x;
    std::cout << "Enter another number: ";
    std::cin >> y;

    std::cout << "Product of the numbers equals " << x * y << "." << std::endl;

    return 0;
}
