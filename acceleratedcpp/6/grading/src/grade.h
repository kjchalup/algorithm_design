#ifndef GUARD_grade
#define GUARD_grade

#include <vector>
#include "Student_info.h"
bool did_all_hw(const Student_info&);
double average_grade(const Student_info&);
double median_grade(const Student_info&);
double optimedian_grade(const Student_info&);
double optimedian_grade(const Student_info&);
double analysis(const data&, double (const Student_info&));

#endif
